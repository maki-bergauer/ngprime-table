export class Product {

    constructor(
        private id: number,
        private code: string,
        private name: string,
        private category: string,
        private price: number,
    ) {}
}
